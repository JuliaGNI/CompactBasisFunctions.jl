var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#References","page":"References","text":"L. N. Trefethen. Approximation Theory and Approximation Practice, Extended Edition (SIAM, Philadelphia, 2019).\n\n\n\nJ. P. Boyd. Chebyshev and Fourier Spectral Methods. Second Edition (Dover, Mineola, New York, 2001).\n\n\n\nC. Canuto, M. Y. Hussaini, A. Quarteroni and T. A. Zang. Spectral Methods: Fundamentals in Single Domains (Springer, Berlin, Heidelberg, 2006).\n\n\n\nJ. E. Marsden and M. West. Discrete mechanics and variational integrators. Acta Numerica 10, 357–514 (2001).\n\n\n\nC. Runge. Über empirische Funktionen und die Interpolation zwischen äquidistanten Ordinaten. Zeitschrift für Mathematik und Physik 46, 224–243 (1901).\n\n\n\nJ. C. Mason and D. C. Handscomb. Chebyshev Polynomials (Chapman and Hall/CRC, Boca Raton, 2003).\n\n\n\nR. T. Farouki. The Bernstein polynomial basis: A centennial retrospective. Computer Aided Geometric Design 29, 379–419 (2012).\n\n\n\nW. Gautschi. Computational aspects of three-term recurrence relations. SIAM Review 9, 24–82 (1967).\n\n\n\nS. Olver, A. Townsend and G. M. Vasil. A practical framework for infinite-dimensional linear algebra. Proceedings of the JuliaCon Conferences 1, 5 (2020).\n\n\n\nJ.-P. Berrut and L. N. Trefethen. Barycentric Lagrange Interpolation. SIAM Review 46, 501–517 (2004).\n\n\n\nN. J. Higham. The Numerical Stability of Barycentric Lagrange Interpolation. IMA Journal of Numerical Analysis 24, 547–556 (2004).\n\n\n\n","title":"References"},{"category":"section","location":"usage/#Usage","page":"Usage","text":"","title":"Usage"},{"category":"section","location":"usage/#Constructing-a-basis","page":"Usage","text":"Every basis is built from the number of basis functions, optionally preceded by an element type, except Lagrange, which is built from its nodes:\n\njulia> Bernstein(4), Legendre(4), ChebyshevT(4), ChebyshevU(4);\n\njulia> Bernstein(Float32, 4), Legendre(BigFloat, 4), Chebyshev{1}(Float32, 4);\n\njulia> Lagrange([0.0, 0.25, 1.0]), LagrangeGauß(4), LagrangeLobatto(4);\n\nThe element type defaults to Float64.","title":"Constructing a basis"},{"category":"section","location":"usage/#Evaluation","page":"Usage","text":"A basis behaves like a matrix with one continuous axis, so evaluation is indexing: the first index is the point, the second the basis function.\n\njulia> b = Legendre(3);\n\njulia> b[0.5, 1]                     # one point, one basis function\n0.0\n\njulia> b(0.5, 1)                     # the same, as a function call\n0.0\n\njulia> b[0.5, :]                     # one point, all basis functions\n3-element OffsetArray(::Vector{Float64}, 0:2) with eltype Float64 with indices 0:2:\n  1.0\n  0.0\n -1.118033988749895\n\njulia> b[[0.25, 0.75], 1]            # several points, one basis function\n2-element Vector{Float64}:\n -0.8660254037844386\n  0.8660254037844386\n\nIndexing with two collections gives the full matrix of values, points along the rows and basis functions along the columns:\n\njulia> b[[0.0, 0.5, 1.0], :]\n3×3 OffsetArray(::Matrix{Float64}, 1:3, 0:2) with eltype Float64 with indices 1:3×0:2:\n 1.0  -1.73205   2.23607\n 1.0   0.0      -1.11803\n 1.0   1.73205   2.23607","title":"Evaluation"},{"category":"section","location":"usage/#Indexing-conventions","page":"Usage","text":"Bernstein, Chebyshev and Legendre index their basis functions from 0; Lagrange indexes from 1. Use eachindex and this never matters:\n\njulia> eachindex(Legendre(3))\nOffsetArrays.IdOffsetRange(values=0:2, indices=0:2)\n\njulia> eachindex(Lagrange([0.0, 0.5, 1.0]))\nSOneTo(3)","title":"Indexing conventions"},{"category":"section","location":"usage/#Evaluation-outside-the-domain","page":"Usage","text":"axes(b, 1) is Inclusion(0..1) for every basis, but indexing does not check it. Outside the interval the polynomial is extrapolated:\n\njulia> b = Bernstein(2);\n\njulia> b[2.0, 0], b[2.0, 1]          # B₀(x) = 1-x and B₁(x) = x, evaluated at x = 2\n(-1.0, 2.0)\n\nThis is deliberate — it is what makes the bases usable as local building blocks on an element of a larger mesh — but note that guarantees which hold on 01, such as Bernstein positivity and the convex-hull property, do not survive outside it.","title":"Evaluation outside the domain"},{"category":"section","location":"usage/#Accessors","page":"Usage","text":"julia> b = ChebyshevT(4);\n\njulia> nbasis(b), order(b), degree(b)\n(4, 4, 3)\n\njulia> nnodes(b)\n4\n\njulia> length(nodes(b)) == length(grid(b)) == nnodes(b)\ntrue\n\naccessor meaning defined for\nnbasis number of basis functions all\norder == nbasis; polynomials of degree < order are exact all\ndegree == nbasis - 1, the highest degree spanned all\nbasis the basis functions as callables all\nnodes the nodes nodal bases only\nnnodes number of nodes nodal bases only\ngrid same as nodes, under ContinuumArrays' name nodal bases only\n\nThe modal bases have no nodes, and say so:\n\njulia> nodes(Bernstein(4))\nERROR: Bernstein is a modal basis and has no nodes, so nodes is not defined for it.\n[...]\n\nSee Nodal and modal bases.","title":"Accessors"},{"category":"section","location":"usage/#Derivatives","page":"Usage","text":"The derivative operator comes from ContinuumArrays and is applied to the basis by multiplication. The product is lazy: it stores the basis and evaluates on indexing.\n\njulia> b = Legendre(3);\n\njulia> d = Derivative(axes(b, 1));\n\njulia> d * b isa LegendreDerivative\ntrue\n\njulia> b' isa LegendreDerivative        # adjoint is the same thing\ntrue\n\njulia> (d*b)[0.5, 2]\n0.0\n\njulia> (d*b)[1.0, 2]           # 6√5: the √(2j+1) of the basis carries to its derivative\n13.416407864998739\n\nIndexing a derivative supports the same four forms as the basis itself: [x, j], [x, :], [X, j] and [X, :].\n\nEach basis has its own derivative type — BernsteinDerivative, ChebyshevDerivative, LagrangeDerivative, LegendreDerivative — which is what makes the formula dispatch on the family.","title":"Derivatives"},{"category":"section","location":"usage/#Expansions","page":"Usage","text":"A function in a basis is the product of the basis with a coefficient vector, which is again lazy and materialises on evaluation:\n\njulia> b = LagrangeLobatto(3);\n\njulia> f(x) = 1 + x^2;\n\njulia> c = [f(x) for x in nodes(b)];            # nodal basis: coefficients are values\n\njulia> sum(c[j] * b[0.3, j] for j in eachindex(b)) ≈ f(0.3)\ntrue\n\nFor a modal basis the coefficients have to be computed. With Legendre that is one quadrature sum per coefficient, since the basis is orthonormal:\n\njulia> b = Legendre(4);  quad = GaussLegendreQuadrature(10);\n\njulia> f(x) = x^3 - x;\n\njulia> c = [sum(weights(quad)[k] * f(nodes(quad)[k]) * b[nodes(quad)[k], j]\n                for k in eachindex(nodes(quad))) for j in eachindex(b)];\n\njulia> sum(c[j] * b[0.3, j] for j in eachindex(b)) ≈ f(0.3)   # c is indexed like b\ntrue","title":"Expansions"},{"category":"section","location":"usage/#Element-types-and-precision","page":"Usage","text":"The element type propagates through every internal quantity, so an arbitrary-precision basis really carries its precision:\n\njulia> setprecision(BigFloat, 256) do\n           b = Lagrange(gauss_legendre_nodes(BigFloat, 4))\n           eltype(b.denom), eltype(b.diffs)\n       end\n(BigFloat, BigFloat)\n\nEvaluation is carried out in the wider of the basis's element type and the type of the point, so the precision of the argument limits nothing. A Float64 point on a BigFloat basis is converted first, and gives exactly what its own exact BigFloat value gives:\n\njulia> setprecision(BigFloat, 256) do\n           b = Legendre(BigFloat, 6)\n           b[0.3, 5] == b[BigFloat(0.3), 5], typeof(b[0.3, 5])\n       end\n(true, BigFloat)\n\nThe promotion only ever widens. A point more precise than the basis keeps its own precision, since nothing is gained by rounding it down:\n\njulia> typeof(Legendre(6)[BigFloat(3) / 10, 5])\nBigFloat\n\nFor Chebyshev the element type must additionally be able to represent the nodes, so an integer type generally fails at construction; see Element type.","title":"Element types and precision"},{"category":"section","location":"usage/#Working-alongside-QuadratureRules-and-ContinuumArrays","page":"Usage","text":"nodes, nnodes and order are the same generic functions that QuadratureRules extends — both packages import them from GeometricBase — so a basis and a quadrature rule can be used together without qualifying anything:\n\njulia> using QuadratureRules\n\njulia> order(Legendre(3)), order(GaussLegendreQuadrature(3))\n(3, 6)\n\njulia> nnodes(ChebyshevT(4)), nnodes(GaussLegendreQuadrature(3))\n(4, 3)\n\ngrid is likewise ContinuumArrays' own function, not a second one of the same name.\n\nwarning: `basis` is ambiguous with ContinuumArrays\nContinuumArrays exports its own basis, which means something different: for a basis object it returns the basis itself, where this package returns the collection of basis functions. Loading both with using therefore leaves the name ambiguous:using ContinuumArrays, CompactBasisFunctions\nbasis(Legendre(3))   # UndefVarError: `basis` not definedQualify it as CompactBasisFunctions.basis, or import the one that is wanted with import CompactBasisFunctions: basis.","title":"Working alongside QuadratureRules and ContinuumArrays"},{"category":"section","location":"library/#Library","page":"Library","text":"The complete API of CompactBasisFunctions.jl. See Usage for how these fit together and Polynomial Approximation for the underlying theory.\n\n","title":"Library"},{"category":"section","location":"library/#Generic-API","page":"Library","text":"Every basis is a subtype of ContinuumArrays' Basis, and answers the accessors below. The methods shown here are the fallbacks on the abstract type, which report that a concrete basis has not implemented them; the per-family pages document what each one returns.\n\nbasis, degree, nodes, nnodes and order are imported from GeometricBase, and grid from ContinuumArrays, so that the packages of the ecosystem extend one generic function per accessor rather than defining one each. nbasis is this package's own.","title":"Generic API"},{"category":"section","location":"library/#Basis-functions","page":"Library","text":"The four families, in the order the manual introduces them.","title":"Basis functions"},{"category":"section","location":"library/#Lagrange","page":"Library","text":"","title":"Lagrange"},{"category":"section","location":"library/#Chebyshev","page":"Library","text":"","title":"Chebyshev"},{"category":"section","location":"library/#Legendre","page":"Library","text":"","title":"Legendre"},{"category":"section","location":"library/#Bernstein","page":"Library","text":"","title":"Bernstein"},{"category":"section","location":"library/#Derivative-types","page":"Library","text":"Applying Derivative(axes(b,1)) to a basis produces a lazy product, one type per family, that evaluates the derivative on indexing. See Derivatives for how they are used.","title":"Derivative types"},{"category":"section","location":"library/#Vandermonde-matrices","page":"Library","text":"Not exported; reach them as CompactBasisFunctions.vandermonde_matrix and CompactBasisFunctions.vandermonde_matrix_inverse.","title":"Vandermonde matrices"},{"category":"function","location":"library/#GeometricBase.basis","page":"Library","text":"basis(b::Basis)\n\nReturn the collection of basis functions of b, indexed as b itself is.\n\nEach element is a callable evaluating one basis function, so that basis(b)[j](x) == b[x,j]. Prefer indexing b directly; this accessor exists for the rare case that the individual functions are needed as values.\n\nNote that ContinuumArrays exports a different basis, which for a basis object returns the basis itself. Loading both packages with using therefore makes the name ambiguous; qualify it, or import the one that is wanted.\n\n\n\n\n\n","title":"GeometricBase.basis"},{"category":"function","location":"library/#CompactBasisFunctions.nbasis","page":"Library","text":"nbasis(b::Basis)\n\nReturn the number of basis functions of b, i.e. the length of basis.\n\nThis equals nnodes for the nodal bases, where each basis function belongs to one node, and is defined for the modal bases too, where nnodes is not.\n\n\n\n\n\n","title":"CompactBasisFunctions.nbasis"},{"category":"function","location":"library/#GeometricBase.nodes","page":"Library","text":"nodes(b::Basis)\n\nReturn the nodes of b, the points x_i in 01 that its construction is based on.\n\nDefined for the nodal bases Lagrange and Chebyshev. The modal bases Bernstein and Legendre have no nodes and throw an error; see Nodal and modal bases.\n\nSee also nnodes for their number and grid for the same vector under the name ContinuumArrays uses.\n\n\n\n\n\n","title":"GeometricBase.nodes"},{"category":"function","location":"library/#GeometricBase.nnodes","page":"Library","text":"nnodes(b::Basis)\n\nReturn the number of nodes of b, i.e. the length of nodes.\n\nDefined only for the nodal bases; see nodes.\n\n\n\n\n\n","title":"GeometricBase.nnodes"},{"category":"function","location":"library/#GeometricBase.order","page":"Library","text":"order(b::Basis)\n\nReturn the order of b, the number of basis functions, hence the number of coefficients an expansion in b has.\n\norder(b) == nbasis(b) and order(b) == degree(b) + 1 for every basis here. The name is the one the ecosystem uses for the accuracy of an approximation: a basis of order p spans the polynomials of degree le p-1 and so reproduces them exactly.\n\n\n\n\n\n","title":"GeometricBase.order"},{"category":"function","location":"library/#GeometricBase.degree","page":"Library","text":"degree(b::Basis)\n\nReturn the degree of b, the highest polynomial degree it spans.\n\ndegree(b) == order(b) - 1 == nbasis(b) - 1 for every basis here.\n\n\n\n\n\n","title":"GeometricBase.degree"},{"category":"type","location":"library/#CompactBasisFunctions.Lagrange","page":"Library","text":"Lagrange(x)\nLagrange{T}(x)\n\nThe Lagrange basis on the nodes x, generally taken in the interval 01,\n\nell_j(x) = prod_i neq j fracx - x_ix_j - x_i \n\nindexed from 1, unlike the other three bases, which are indexed from 0.\n\nThis is the cardinal basis of the nodes: ell_j(x_i) = delta_ij, so the coefficients of an expansion are the values of the function at the nodes, and no linear system is needed to interpolate. The basis also forms a partition of unity, sum_j ell_j(x) = 1.\n\njulia> l = Lagrange([0.0, 0.5, 1.0]);\n\njulia> all(l[nodes(l)[i], j] ≈ (i == j) for i in 1:3, j in 1:3)   # ℓⱼ(xᵢ) = δᵢⱼ\ntrue\n\njulia> sum(l[0.3, j] for j in eachindex(l)) ≈ 1                   # partition of unity\ntrue\n\nThe nodes must be distinct and finite, since the denominators are products of their differences; anything that leaves such a product zero or non-finite throws an ArgumentError. That covers a repeated node, the pair 0.0 and -0.0, whose difference is zero although the two are not isequal, and a NaN or Inf node. They need not be sorted, nor confined to 01, although the declared domain is 01.\n\nWhich nodes to use matters: equidistant nodes make high-degree interpolation diverge near the endpoints (the Runge phenomenon), so the Gauß-Legendre and Lobatto-Legendre node sets are provided as LagrangeGauß and LagrangeLobatto; see Choice of nodes.\n\nThe element type T is taken from the nodes, and all internal quantities are formed in it, so an arbitrary-precision basis carries its full precision:\n\njulia> setprecision(BigFloat, 256) do\n           l = Lagrange(BigFloat[0, 1//4, 1])\n           abs(sum(l[BigFloat(1)/3, j] for j in eachindex(l)) - 1) < 1e-70\n       end\ntrue\n\nSee also Chebyshev for the other nodal basis, and Lagrange basis for the full discussion.\n\n\n\n\n\n","title":"CompactBasisFunctions.Lagrange"},{"category":"function","location":"library/#CompactBasisFunctions.LagrangeGauß","page":"Library","text":"LagrangeGauß(n)\n\nThe Lagrange basis on the n Gauß-Legendre nodes of 01.\n\nThese lie strictly inside the interval, which suits a basis whose expansion is integrated rather than matched at the boundary. Compare LagrangeLobatto, whose nodes include the endpoints.\n\njulia> nodes(LagrangeGauß(2)) ≈ [(1 - 1/sqrt(3)) / 2, (1 + 1/sqrt(3)) / 2]\ntrue\n\n\n\n\n\n","title":"CompactBasisFunctions.LagrangeGauß"},{"category":"function","location":"library/#CompactBasisFunctions.LagrangeLobatto","page":"Library","text":"LagrangeLobatto(n)\n\nThe Lagrange basis on the n Lobatto-Legendre nodes of 01.\n\nThese include both endpoints, so an expansion has coefficients that are the boundary values themselves — what a method needs when it has to impose or read off conditions there. Compare LagrangeGauß, whose nodes lie strictly inside.\n\njulia> nodes(LagrangeLobatto(3)) == [0.0, 0.5, 1.0]\ntrue\n\n\n\n\n\n","title":"CompactBasisFunctions.LagrangeLobatto"},{"category":"type","location":"library/#CompactBasisFunctions.Chebyshev","page":"Library","text":"Chebyshev{kind}(n)\nChebyshev{kind}(T, n)\nChebyshevT(n)     # kind = 1\nChebyshevU(n)     # kind = 2\n\nChebyshev basis of the first (kind = 1) or second (kind = 2) kind on the interval 01, of n functions, i.e. of degree p = n-1, indexed from 0.\n\nThe basis functions are the Chebyshev polynomials evaluated at 2x-1, and the nodes are the Chebyshev points shifted onto [0..1] and returned in ascending order.\n\nT_j(tildex) = cos(j arccos tildex)  qquad\nU_j(tildex) = fracsinbig((j+1) arccos tildexbig)sin(arccos tildex) \nqquad tildex = 2x-1 \n\nboth satisfying the recurrence phi_j = 2tildex phi_j-1 - phi_j-2, which is how they are evaluated; see The reference interval for the shift and its chain-rule factor 2 on derivatives.\n\nUnlike the modal bases, a Chebyshev basis carries nodes: the Chebyshev points of the corresponding kind, from QuadratureRules, shifted onto 01 and ascending. Those of the first kind are the roots of T_n and lie strictly inside the interval; those of the second kind are the extrema of T_n-1 and include both endpoints, which is why kind = 2 requires n ≥ 2.\n\njulia> t = ChebyshevT(3);\n\njulia> t[0.0, 2], t[0.5, 2], t[1.0, 2]      # T₂(x̃) at x̃ = -1, 0, +1\n(1.0, -1.0, 1.0)\n\njulia> nodes(ChebyshevU(3))                 # includes both endpoints\n3-element Vector{Float64}:\n 0.0\n 0.5\n 1.0\n\njulia> all(x -> x ∈ axes(t, 1), nodes(t))   # the nodes lie in the domain\ntrue\n\nT is the element type of the nodes, and must be able to represent them. This means a floating-point type in general; an integer-like T only works in the special cases where the nodes are exactly representable, such as ChebyshevU(Integer, 2), whose nodes are 0 and 1. Otherwise the constructor throws an InexactError.\n\nSee also Chebyshev basis for the full discussion, and Lagrange for the other nodal basis.\n\n\n\n\n\n","title":"CompactBasisFunctions.Chebyshev"},{"category":"type","location":"library/#CompactBasisFunctions.Legendre","page":"Library","text":"Legendre(n)\nLegendre(T, n)\n\nThe Legendre basis of n functions, i.e. of degree p = n-1, on the interval 01,\n\nL_j(x) = sqrt2j+1  P_j(2x-1)  qquad j = 0 dots p \n\nindexed from 0. T is the element type and defaults to Float64.\n\nThe Legendre polynomials P_j are defined on -1+1, so the argument is shifted by tildex = 2x-1; see The reference interval.\n\nThe basis is modal: its coefficients are those of an expansion rather than values at points, so it has no nodes and no grid, and those accessors throw.\n\nThe factor sqrt2j+1 makes the basis orthonormal on 01: since int_0^1 P_i(2x-1) P_j(2x-1)  dx = delta_ij  (2j+1),\n\nint_0^1 L_i(x)  L_j(x)  dx = delta_ij \n\nso the mass matrix is the identity and the coefficients of a projection are just the inner products against the basis functions.\n\njulia> l = Legendre(3);\n\njulia> l[0.5, 0], l[0.5, 1], l[0.5, 2]      # the midpoint is x̃ = 0\n(1.0, 0.0, -1.118033988749895)\n\njulia> l[1.0, 1] ≈ sqrt(3)                  # L₁(1) = √3 P₁(1) = √3\ntrue\n\njulia> using QuadratureRules\n\njulia> quad = GaussLegendreQuadrature(8);\n\njulia> sum(weights(quad)[k] * l[nodes(quad)[k], 1]^2 for k in eachindex(nodes(quad))) ≈ 1\ntrue\n\nThe derivative follows from differentiating Bonnet's recurrence, with the chain-rule factor 2 from the shift, and is obtained as Derivative(axes(l,1)) * l; see Derivatives.\n\nSee also Bernstein for the other modal basis, and Legendre basis for the full discussion.\n\n\n\n\n\n","title":"CompactBasisFunctions.Legendre"},{"category":"type","location":"library/#CompactBasisFunctions.Bernstein","page":"Library","text":"Bernstein(n)\nBernstein(T, n)\n\nThe Bernstein basis of n functions, i.e. of degree p = n-1, on the interval 01,\n\nB_jp(x) = binompj  x^j  (1-x)^p-j  qquad j = 0 dots p \n\nindexed from 0. T is the element type and defaults to Float64.\n\nThe basis is modal: its coefficients are not values at points, so it has no nodes and no grid, and those accessors throw. The coefficients are the control values of a Bézier curve.\n\nOn 01 the basis functions are non-negative and form a partition of unity, sum_j B_jp(x) = 1, so an expansion is a convex combination of its coefficients and therefore lies in their convex hull. The first and last function interpolate the endpoints, B_0p(0) = B_pp(1) = 1, while every other function vanishes at both.\n\njulia> b = Bernstein(3);\n\njulia> b[0.0, 0], b[0.5, 0], b[1.0, 0]     # B₀ interpolates the left endpoint\n(1.0, 0.25, 0.0)\n\njulia> b[0.0, 2], b[0.5, 2], b[1.0, 2]     # B₂ interpolates the right endpoint\n(0.0, 0.25, 1.0)\n\njulia> sum(b[0.3, j] for j in eachindex(b)) ≈ 1\ntrue\n\nThe derivative is again a Bernstein expansion, of one degree less,\n\nB_jp(x) = p  big( B_j-1p-1(x) - B_jp-1(x) big) \n\nand is obtained as Derivative(axes(b,1)) * b; see Derivatives.\n\nSee also Legendre for the other modal basis, and Bernstein basis for the full discussion.\n\n\n\n\n\n","title":"CompactBasisFunctions.Bernstein"},{"category":"type","location":"library/#CompactBasisFunctions.BernsteinDerivative","page":"Library","text":"BernsteinDerivative\n\nThe type of Derivative(axes(b,1)) * b for a Bernstein basis b, equivalently of b'.\n\nA lazy product: it stores the basis and evaluates the derivative on indexing, so (d*b)[x,j] is B_jp(x). See Derivatives.\n\n\n\n\n\n","title":"CompactBasisFunctions.BernsteinDerivative"},{"category":"type","location":"library/#CompactBasisFunctions.ChebyshevDerivative","page":"Library","text":"ChebyshevDerivative\n\nThe type of Derivative(axes(b,1)) * b for a Chebyshev basis b of either kind, equivalently of b'.\n\nA lazy product: it stores the basis and evaluates the derivative on indexing. The two kinds need different formulas, so the evaluation dispatches on the narrower ChebyshevTDerivative and ChebyshevUDerivative. See Derivatives.\n\n\n\n\n\n","title":"CompactBasisFunctions.ChebyshevDerivative"},{"category":"type","location":"library/#CompactBasisFunctions.ChebyshevTDerivative","page":"Library","text":"ChebyshevTDerivative\n\nChebyshevDerivative narrowed to the first kind, evaluated through T_j = j  U_j-1.\n\n\n\n\n\n","title":"CompactBasisFunctions.ChebyshevTDerivative"},{"category":"type","location":"library/#CompactBasisFunctions.ChebyshevUDerivative","page":"Library","text":"ChebyshevUDerivative\n\nChebyshevDerivative narrowed to the second kind, evaluated through the differentiated recurrence rather than the closed form, which is singular at the endpoints.\n\n\n\n\n\n","title":"CompactBasisFunctions.ChebyshevUDerivative"},{"category":"type","location":"library/#CompactBasisFunctions.LagrangeDerivative","page":"Library","text":"LagrangeDerivative\n\nThe type of Derivative(axes(l,1)) * l for a Lagrange basis l, equivalently of l'.\n\nA lazy product: it stores the basis and evaluates the derivative on indexing, from the node differences the basis caches. See Derivatives.\n\n\n\n\n\n","title":"CompactBasisFunctions.LagrangeDerivative"},{"category":"type","location":"library/#CompactBasisFunctions.LegendreDerivative","page":"Library","text":"LegendreDerivative\n\nThe type of Derivative(axes(l,1)) * l for a Legendre basis l, equivalently of l'.\n\nA lazy product: it stores the basis and evaluates the derivative on indexing, from the differentiated Bonnet recurrence with the chain-rule factor 2 of the shift onto 01. See Derivatives.\n\n\n\n\n\n","title":"CompactBasisFunctions.LegendreDerivative"},{"category":"function","location":"library/#CompactBasisFunctions.vandermonde_matrix","page":"Library","text":"vandermonde_matrix(x)\n\nThe Vandermonde matrix of the nodes x, V_ij = x_i^j-1.\n\nV is the matrix of the monomial basis evaluated at the nodes, so V * c are the values at the nodes of the polynomial with monomial coefficients c, and V \\ y are the monomial coefficients of the polynomial interpolating the values y.\n\njulia> vandermonde_matrix([0.0, 0.5, 1.0])\n3×3 Matrix{Float64}:\n 1.0  0.0  0.0\n 1.0  0.5  0.25\n 1.0  1.0  1.0\n\nSee also vandermonde_matrix_inverse and The Vandermonde matrix.\n\n\n\n\n\n","title":"CompactBasisFunctions.vandermonde_matrix"},{"category":"function","location":"library/#CompactBasisFunctions.vandermonde_matrix_inverse","page":"Library","text":"vandermonde_matrix_inverse(x)\n\nThe inverse of vandermonde_matrix, computed in closed form.\n\nV is factored as V^-1 = U L with L lower and U upper triangular, whose entries are known explicitly from the nodes: L holds the reciprocals of the products of node differences that also appear in the Lagrange cardinal functions, and U is built by a two-term recurrence. This avoids a general factorisation of a matrix that is notoriously ill-conditioned, though the result is still limited by that conditioning.\n\nV⁻¹ * y are the monomial coefficients of the polynomial interpolating the values y at the nodes x.\n\njulia> vandermonde_matrix_inverse([0.0, 0.5, 1.0])\n3×3 Matrix{Float64}:\n  1.0   0.0   0.0\n -3.0   4.0  -1.0\n  2.0  -4.0   2.0\n\nThe nodes must be distinct, as for Lagrange; repeated nodes divide by zero.\n\n\n\n\n\n","title":"CompactBasisFunctions.vandermonde_matrix_inverse"},{"category":"section","location":"approximation/#Polynomial-Approximation","page":"Polynomial Approximation","text":"The problem this package addresses is to represent a function f on an interval by a finite combination of simpler functions,\n\nf(x) approx sum_j c_j  phi_j(x) \n\nwhere the phi_j are the basis functions and the c_j the coefficients. Once such a representation is fixed, the operations one actually wants — differentiating, integrating, taking inner products, solving a variational problem — become linear algebra on the coefficients, and the only thing that varies from one method to the next is which basis was chosen and why.\n\nEverything in this package is polynomial: each phi_j is a polynomial of degree at most p, so the four bases all span the same space, the polynomials of degree le p. They differ only in which basis of that space they provide. That sounds like a distinction without a difference, and mathematically it is; numerically and practically it is not. A basis determines what the coefficients mean, how expensive and how stable it is to evaluate them, and how easily boundary conditions, positivity, or orthogonality can be imposed.\n\nFor the general theory see [1] and, for the spectral-methods perspective that motivates most of the choices here, [2] and [3].","title":"Polynomial Approximation"},{"category":"section","location":"approximation/#Interpolation-and-projection","page":"Polynomial Approximation","text":"There are two natural ways to pick the coefficients.\n\nInterpolation demands agreement at a finite set of nodes x_i:\n\nsum_j c_j  phi_j(x_i) = f(x_i) qquad textfor all  i \n\nThis is a linear system with the matrix Phi_ij = phi_j(x_i). With as many nodes as basis functions it has a unique solution whenever the nodes are distinct, and the quality of the result depends on the nodes far more than on the basis.\n\nProjection demands agreement in an integral sense, minimising  f - sum_j c_j phi_j  in the L^2 norm, which gives the normal equations\n\nsum_j M_ij  c_j = int f(x)  phi_i(x)  dx  qquad\nM_ij = int phi_i(x)  phi_j(x)  dx \n\nwith M the mass matrix of the basis. Projection needs no nodes but it needs integrals, which in practice are evaluated by quadrature — which is why this package is built on QuadratureRules.jl and why the two share their nodes, weights and order accessors.\n\nInterpolation is cheap and local; projection is optimal in the norm it minimises. Variational integrators, the application this package was written for, use both: a Lagrange or Chebyshev basis to interpolate a trajectory through nodes, and a quadrature rule to approximate the action integral [4].","title":"Interpolation and projection"},{"category":"section","location":"approximation/#Nodal-and-modal-bases","page":"Polynomial Approximation","text":"The distinction that organises this package is whether a basis comes with nodes.\n\nA nodal basis is built from a set of points, and is cardinal with respect to them: phi_j(x_i) = delta_ij. The interpolation matrix Phi is then the identity, so the coefficients simply are the function values, c_j = f(x_j), and interpolation costs nothing. Lagrange is the nodal basis in its purest form, and Chebyshev carries the Chebyshev points as its nodes.\n\nA modal basis has no nodes. Its coefficients are the coefficients of an expansion — amplitudes of modes — and recovering them from function values requires solving a system or computing integrals. Legendre and Bernstein are modal.\n\nThis is why nodes, nnodes and grid are defined for the first group and throw an informative error for the second:\n\njulia> nnodes(LagrangeLobatto(3))\n3\n\njulia> nodes(Legendre(3))\nERROR: Legendre is a modal basis and has no nodes, so nodes is not defined for it.\n[...]\n\nnbasis, order and degree are defined for all four.","title":"Nodal and modal bases"},{"category":"section","location":"approximation/#The-reference-interval","page":"Polynomial Approximation","text":"Every basis in this package is defined on the interval 01, and axes(b, 1) is Inclusion(0..1) for all of them. QuadratureRules follows the same convention, so nodes, weights and basis functions compose without rescaling.\n\nThe classical theory, on the other hand, lives on the symmetric interval -1+1, which is where the Chebyshev and Legendre polynomials are defined and where their orthogonality relations take their usual form. The two views are related by the affine map\n\ntildex = 2x - 1  qquad x in 01 Longleftrightarrow tildex in -1+1 \n\nand this package evaluates the classical polynomial at tildex. Differentiating brings in the chain-rule factor\n\nfracddx  phi(2x-1) = 2  phi(2x-1) \n\nwhich is why every derivative of a shifted basis carries a factor 2. Bernstein and Lagrange need no shift: the former is defined on 01 to begin with, and the latter is defined by whatever nodes it is given.\n\nAn interval ab other than the reference one is handled by the caller, by composing with x = a + (b-a)xi; a derivative then picks up 1(b-a).","title":"The reference interval"},{"category":"section","location":"approximation/#Choice-of-nodes","page":"Polynomial Approximation","text":"For a nodal basis, the nodes matter more than anything else. Interpolating at equidistant nodes is unstable: as the degree grows, the interpolant develops oscillations near the ends of the interval that grow without bound even for perfectly smooth functions. This is the Runge phenomenon [5], and the underlying reason is that the Lebesgue constant of equidistant nodes grows exponentially in the degree.\n\nNodes clustered towards the endpoints avoid this. Two families are used here:\n\nthe Chebyshev points, the roots or extrema of a Chebyshev polynomial, whose Lebesgue constant grows only logarithmically — these are the nodes of Chebyshev;\nthe Gauß- and Lobatto-Legendre nodes, the points of the corresponding quadrature rules, available as LagrangeGauß and LagrangeLobatto.\n\nThe choice between the last two is usually about the boundary. Gauß nodes lie strictly inside the interval and integrate to higher order; Lobatto nodes include both endpoints, so the coefficients of an expansion include the boundary values themselves — what a method needs when it has to impose or read off conditions there.","title":"Choice of nodes"},{"category":"section","location":"approximation/#The-Vandermonde-matrix","page":"Polynomial Approximation","text":"For the monomial basis phi_j(x) = x^j-1 the interpolation matrix is the Vandermonde matrix V_ij = x_i^j-1, available as vandermonde_matrix. It maps monomial coefficients to values at the nodes, so its inverse maps values to monomial coefficients, and composing the two directions converts between the nodal and monomial representations of the same polynomial.\n\njulia> V = vandermonde_matrix([0.0, 0.5, 1.0]);\n\njulia> V * [1.0, 0.0, 1.0]                # values of 1 + x² at the nodes\n3-element Vector{Float64}:\n 1.0\n 1.25\n 2.0\n\njulia> vandermonde_matrix_inverse([0.0, 0.5, 1.0]) * [1.0, 1.25, 2.0]\n3-element Vector{Float64}:\n 1.0\n 0.0\n 1.0\n\nvandermonde_matrix_inverse computes the inverse in closed form, as a product of a lower and an upper triangular factor whose entries are known explicitly from the nodes, rather than by a general factorisation.\n\nThat said, the Vandermonde matrix is the standard example of an ill-conditioned matrix: its condition number grows exponentially in the number of nodes, whatever the nodes are. This is not a defect of the algorithm but of the monomial basis, and it is the practical argument for working in one of the four bases here instead of in coefficients of x^k.","title":"The Vandermonde matrix"},{"category":"section","location":"approximation/#Orthogonality","page":"Polynomial Approximation","text":"A basis is orthogonal when its mass matrix is diagonal, and orthonormal when the mass matrix is the identity. Then projection needs no linear solve at all: the coefficients are just the inner products against the basis functions.\n\nLegendre is orthonormal on 01 by construction. Since int_0^1 P_i(2x-1) P_j(2x-1)  dx = delta_ij(2j+1), scaling the j-th basis function by sqrt2j+1 gives exactly int_0^1 L_i L_j  dx = delta_ij, which is what that factor in the definition is for.\n\njulia> l = Legendre(4);  quad = GaussLegendreQuadrature(8);\n\njulia> M = [sum(weights(quad)[k] * l[nodes(quad)[k], i] * l[nodes(quad)[k], j]\n                for k in eachindex(nodes(quad))) for i in 0:3, j in 0:3];\n\njulia> maximum(abs, M - [i == j for i in 0:3, j in 0:3]) < 1e-14\ntrue\n\nThe Chebyshev polynomials are orthogonal too, but with respect to a weighted inner product, with weight (1-tildex^2)^-12 for the first kind and (1-tildex^2)^+12 for the second [6]; they are not orthogonal in the unweighted L^2 inner product that the mass matrix above uses.","title":"Orthogonality"},{"category":"section","location":"approximation/#Partition-of-unity","page":"Polynomial Approximation","text":"A basis forms a partition of unity when sum_j phi_j(x) = 1 for every x. Then a constant function is represented by constant coefficients, an expansion reproduces constants exactly, and — if the basis functions are also non-negative — the expansion is a convex combination of its coefficients and therefore bounded by them.\n\nBernstein has both properties on 01, which is what makes it the basis of Bézier curves and gives the convex-hull property that computer-aided design relies on [7]. Lagrange forms a partition of unity as well, though its cardinal functions do take negative values.\n\nA useful consequence: differentiating the identity gives sum_j phi_j(x) = 0, so the rows of any derivative matrix sum to zero. Both identities make good tests, precisely because they hold exactly rather than approximately, and the size of the deviation from them measures the accumulated rounding error of the evaluation. This is how the arbitrary-precision behaviour of Lagrange is checked in the test suite.","title":"Partition of unity"},{"category":"section","location":"approximation/#Evaluation-by-recurrence","page":"Polynomial Approximation","text":"Classical orthogonal polynomials are not evaluated from their explicit coefficients, which would be both slow and unstable, but from three-term recurrences [8]:\n\nT_j = 2tildex T_j-1 - T_j-2  qquad\nU_j = 2tildex U_j-1 - U_j-2  qquad\nj P_j = (2j-1) tildex P_j-1 - (j-1) P_j-2 \n\nThese are stable in the upward direction, and this package iterates them upwards, carrying the two previous values. Derivatives are obtained by differentiating the same recurrences term by term and carrying the derivative alongside the value:\n\nP_j = frac(2j-1) P_j-1 + (2j-1) tildex P_j-1 - (j-1) P_j-2j  qquad\nU_j = 2 U_j-1 + 2tildex U_j-1 - U_j-2 \n\nDoing this by recursion rather than iteration is a trap. Each step then descends into two subproblems and recomputes shared subtrees, so the cost of one value grows like varphi^j rather than j. Earlier versions of this package did exactly that, at 45 ms for a single degree-24 Bernstein value; the iterative form takes about 120 ns and grows linearly.\n\nThe Bernstein recurrence\n\nB_jp = (1-x)  B_jp-1 + x  B_j-1p-1\n\nhas two indices, so it cannot be collapsed into a single upward sweep for one j; that basis is evaluated from the closed form B_jp(x) = binompj x^j (1-x)^p-j instead, with the binomial coefficient accumulated multiplicatively over integer partial products; see Evaluation in closed form for where that stops being exact.","title":"Evaluation by recurrence"},{"category":"section","location":"approximation/#Bases-as-quasi-matrices","page":"Polynomial Approximation","text":"The interface follows ContinuumArrays.jl [9]: a basis is a matrix with one continuous and one discrete axis, a quasi-matrix. The continuous axis is the domain, the discrete one indexes the basis functions, so that b[x, j] evaluates phi_j(x) and reads exactly like indexing.\n\nThe payoff is that the linear algebra of the approximation is expressible directly: Derivative(axes(b,1)) * b is the derivative operator applied to the basis, and b * c is the function with coefficients c — both as lazy products, materialising only when indexed. See Usage for what that looks like in practice.","title":"Bases as quasi-matrices"},{"category":"section","location":"lagrange/#Lagrange-basis","page":"Lagrange","text":"","title":"Lagrange basis"},{"category":"section","location":"lagrange/#Definition","page":"Lagrange","text":"Given n distinct nodes x_1 dots x_n, the Lagrange basis is\n\nell_j(x) = prod_i neq j fracx - x_ix_j - x_i  qquad j = 1 dots n \n\na basis of the polynomials of degree le n-1. This is the cardinal basis of the nodes,\n\nell_j(x_i) = delta_ij \n\nwhich is the property everything else follows from.","title":"Definition"},{"category":"section","location":"lagrange/#Domain-and-nodes","page":"Lagrange","text":"Nodal, and the only basis here whose nodes the user supplies. The declared domain is 01, but the nodes are whatever they are given as: they need not be sorted, and nothing forces them into the interval.\n\nNote that Lagrange indexes its basis functions from 1, where the other three bases index from 0. Write loops over eachindex(b) rather than over 0:degree(b) and this never comes up.\n\njulia> l = Lagrange([0.0, 0.5, 1.0]);\n\njulia> eachindex(l)\nSOneTo(3)\n\njulia> nnodes(l), nbasis(l), order(l), degree(l)\n(3, 3, 3, 2)","title":"Domain and nodes"},{"category":"section","location":"lagrange/#Interpolation-is-free","page":"Lagrange","text":"Because the basis is cardinal, the coefficients of an interpolant are the values at the nodes — there is no system to solve. This is the reason to choose a Lagrange basis.\n\njulia> l = LagrangeLobatto(3);\n\njulia> f(x) = 1 + x^2;\n\njulia> c = [f(x) for x in nodes(l)];        # the coefficients are just the values\n\njulia> sum(c[j] * l[0.3, j] for j in eachindex(l)) ≈ f(0.3)\ntrue\n\nThe last line is exact up to rounding because f is a polynomial of degree 2 and the basis spans degree 2.","title":"Interpolation is free"},{"category":"section","location":"lagrange/#Cardinality-and-partition-of-unity","page":"Lagrange","text":"julia> l = Lagrange([0.0, 0.1, 0.7, 1.0]);\n\njulia> all(l[nodes(l)[i], j] ≈ (i == j) for i in 1:4, j in 1:4)\ntrue\n\njulia> sum(l[0.42, j] for j in eachindex(l)) ≈ 1\ntrue\n\nThe cardinal functions do take negative values between the nodes, so unlike Bernstein this partition of unity is not a convex combination.","title":"Cardinality and partition of unity"},{"category":"section","location":"lagrange/#Which-nodes-to-use","page":"Lagrange","text":"Equidistant nodes are a poor choice at higher degree — see Choice of nodes — so two better sets are provided directly:\n\njulia> collect(nodes(LagrangeGauß(3)))      # strictly inside the interval\n3-element Vector{Float64}:\n 0.11270166537925831\n 0.5\n 0.8872983346207417\n\njulia> collect(nodes(LagrangeLobatto(3)))   # includes both endpoints\n3-element Vector{Float64}:\n 0.0\n 0.5\n 1.0\n\nThe nodes are stored in an SVector, hence the collect above; nodes itself returns the static vector.\n\nUse LagrangeGauß when the expansion is going to be integrated, and LagrangeLobatto when the boundary values have to be imposed or read off.","title":"Which nodes to use"},{"category":"section","location":"lagrange/#Derivative","page":"Lagrange","text":"Differentiating the product gives\n\nell_j(x) = sum_l neq j frac1x_j - x_l prod_i neq j l fracx - x_ix_j - x_i \n\nwhich is what LagrangeDerivative evaluates, reusing the table of node differences the basis caches at construction.\n\njulia> l = LagrangeLobatto(3);\n\njulia> d = Derivative(axes(l, 1));\n\njulia> sum((d*l)[0.3, j] for j in eachindex(l)) |> abs < 1e-14      # rows sum to zero\ntrue","title":"Derivative"},{"category":"section","location":"lagrange/#Element-type-and-precision","page":"Lagrange","text":"All internal quantities — the node differences and the reciprocal denominators — are formed in the element type of the nodes, so a BigFloat basis carries its full precision. Before version 0.3.0 those buffers were allocated in Float64 regardless, so a BigFloat basis reported BigFloat while carrying only double precision, in the basis and in its derivative alike.\n\njulia> setprecision(BigFloat, 256) do\n           l = Lagrange(gauss_legendre_nodes(BigFloat, 4))\n           d = Derivative(axes(l, 1))\n           z = BigFloat(1) / 7\n           (abs(sum(l[z, j] for j in eachindex(l)) - 1) < 1e-70,\n            abs(sum((d*l)[z, j] for j in eachindex(l))) < 1e-70)\n       end\n(true, true)","title":"Element type and precision"},{"category":"section","location":"lagrange/#Pitfalls","page":"Lagrange","text":"The nodes must be distinct. The denominators are products of node differences, so a repeated node is a division by zero. This is rejected rather than silently producing Inf:\njulia> Lagrange([0.0, 0.5, 0.5, 1.0])\nERROR: ArgumentError: the nodes of a Lagrange basis must be distinct, got [0.0, 0.5, 0.5, 1.0]\n[...]\nWhat is tested is the differences themselves, not the node list under isequal — the comparison allunique uses, and a different question from the one the denominators ask. 0.0 and -0.0 are distinct under isequal, yet their difference is zero:\njulia> allunique([0.0, -0.0])\ntrue\n\njulia> Lagrange([0.0, -0.0])\nERROR: ArgumentError: the nodes of a Lagrange basis must be distinct, got [0.0, -0.0]\n[...]\nThe nodes must be finite. A NaN or Inf node is isequal to nothing else and so looks perfectly distinct, while poisoning every difference it takes part in:\njulia> Lagrange([0.0, NaN, 1.0])\nERROR: ArgumentError: the nodes of a Lagrange basis must be finite, got [0.0, NaN, 1.0]\n[...]\nThe product of the differences must be representable. Nodes that are distinct and finite can still multiply out to zero or to an infinity — packed into a narrow range the product underflows, spread over a wide one it overflows — and either way the denominator is unusable. The nodes are not at fault there, so the message says so and points at the remedy:\njulia> Lagrange([0.0, 1e160, 2e160, 3e160])\nERROR: ArgumentError: the nodes of a Lagrange basis are distinct and finite, but the product of the differences from node 1 is -Inf in Float64, so the denominator it gives is not usable; rescale the nodes or widen the element type\n[...]\nScaling such a node set to [0,1] and mapping the argument along with it is the way out; a wider element type buys room too, but only a fixed amount of it.\nIndexing starts at 1, unlike the other three bases.\nEquidistant nodes at high degree will diverge; see Choice of nodes.\nThe nodes are stored in an SVector, so the number of nodes is part of the type. Building a basis with very many nodes is therefore a compile-time cost, not just a run-time one.\n\nFor the barycentric reformulation, which is the preferred way to evaluate a Lagrange interpolant at many points and is more stable than the product form, see [10] and [11].","title":"Pitfalls"},{"category":"section","location":"chebyshev/#Chebyshev-basis","page":"Chebyshev","text":"","title":"Chebyshev basis"},{"category":"section","location":"chebyshev/#Definition","page":"Chebyshev","text":"The Chebyshev polynomials of the first and second kind are\n\nT_j(tildex) = cos(jtheta)  qquad\nU_j(tildex) = fracsinbig((j+1)thetabig)sintheta  qquad\ntildex = costheta \n\non tildex in -1+1. Both satisfy the same three-term recurrence,\n\nphi_j = 2tildex  phi_j-1 - phi_j-2 \n\ndiffering only in the starting values: T_0 = 1 T_1 = tildex and U_0 = 1 U_1 = 2tildex. This is how they are evaluated here — iterated upwards, which is the stable direction; see Evaluation by recurrence.\n\nFor the classical theory see [6], and for their role in spectral methods [2].","title":"Definition"},{"category":"section","location":"chebyshev/#Domain-and-the-shift","page":"Chebyshev","text":"The basis functions are the polynomials evaluated at tildex = 2x-1, so the basis lives on 01 like everything else in this package; see The reference interval.\n\njulia> t = ChebyshevT(3);\n\njulia> axes(t, 1)\nInclusion(0 .. 1)\n\njulia> t[0.0, 2], t[0.5, 2], t[1.0, 2]      # T₂(x̃) at x̃ = -1, 0, +1\n(1.0, -1.0, 1.0)\n\nwarning: Changed in 0.3.0\nBefore version 0.3.0 the nodes and basis functions lived on -1+1 while axes already declared Inclusion(0..1), so grid(C) did not lie inside axes(C,1). Every node, basis function and derivative value therefore changed in 0.3.0, and the nodes are now returned in ascending rather than descending order.","title":"Domain and the shift"},{"category":"section","location":"chebyshev/#Nodes","page":"Chebyshev","text":"Unlike the modal bases, a Chebyshev basis carries nodes: the Chebyshev points of the matching kind, obtained from QuadratureRules, shifted onto 01 and returned ascending.\n\nThose of the first kind are the roots of T_n, tildex_i = cosbig((2i-1)pi2nbig), and lie strictly inside the interval. Those of the second kind are the extrema of T_n-1, tildex_i = cosbig((i-1)pi(n-1)big), and include both endpoints.\n\njulia> nodes(ChebyshevT(3))                 # strictly interior\n3-element Vector{Float64}:\n 0.06698729810778067\n 0.5\n 0.9330127018922193\n\njulia> nodes(ChebyshevU(3))                 # includes 0 and 1\n3-element Vector{Float64}:\n 0.0\n 0.5\n 1.0\n\njulia> issorted(nodes(ChebyshevT(5)))\ntrue\n\nBecause the second-kind points are the extrema of T_n-1, they need at least two points to exist, so ChebyshevU(1) is an error.\n\nThese are the node sets that make high-degree interpolation well behaved: their Lebesgue constant grows logarithmically in the degree rather than exponentially, which is what defeats the Runge phenomenon.","title":"Nodes"},{"category":"section","location":"chebyshev/#Derivative","page":"Chebyshev","text":"The first kind differentiates into the second,\n\nT_j(tildex) = j  U_j-1(tildex) \n\nwhich is division-free and used directly. The second kind has the closed form\n\nU_j(tildex) = frac(j+1) T_j+1(tildex) - tildex  U_j(tildex)tildex^2 - 1 \n\nwhich is 00 at tildex = pm 1 — that is, at both endpoints of 01. It is therefore not used; the derivative is obtained from the differentiated recurrence\n\nU_j = 2 U_j-1 + 2tildex  U_j-1 - U_j-2 \n\ncarried alongside U_j itself, which has no singularity anywhere. Both carry the chain-rule factor 2 from the shift.\n\njulia> u = ChebyshevU(4);  d = Derivative(axes(u, 1));\n\njulia> [(d*u)[1.0, i] for i in eachindex(u)]    # finite at the endpoint\n4-element OffsetArray(::Vector{Float64}, 0:3) with eltype Float64 with indices 0:3:\n  0.0\n  4.0\n 16.0\n 40.0\n\njulia> [(d*u)[1.0, i] for i in eachindex(u)] == [2i*(i+1)*(i+2)//3 for i in eachindex(u)]\ntrue\n\nwarning: Fixed in 0.3.0\nEarlier versions evaluated the closed form, so (d*ChebyshevU(n))[0.0, j] and [1.0, j] returned NaN for every j.","title":"Derivative"},{"category":"section","location":"chebyshev/#Orthogonality","page":"Chebyshev","text":"The Chebyshev polynomials are orthogonal with respect to a weighted inner product, with weight (1-tildex^2)^-12 for the first kind and (1-tildex^2)^+12 for the second. They are not orthogonal in the unweighted L^2 inner product, so the mass matrix of this basis is not diagonal — unlike Legendre.","title":"Orthogonality"},{"category":"section","location":"chebyshev/#Element-type","page":"Chebyshev","text":"T is the element type of the nodes and must be able to represent them, which in general means a floating-point type. The conversion happens at construction, so a type that cannot represent the nodes fails there rather than on first use:\n\njulia> nodes(ChebyshevU(Integer, 2))        # exactly 0 and 1, so this works\n2-element Vector{Integer}:\n 0\n 1\n\njulia> try ChebyshevT(Integer, 3) catch e; typeof(e) end\nInexactError\n\nThe node values are computed in BigFloat by QuadratureRules and rounded once to T, so they are correctly rounded whatever T is.\n\nT also governs the arithmetic of an evaluation, which runs in the wider of T and the type of the point; see Element types and precision.","title":"Element type"},{"category":"section","location":"chebyshev/#Pitfalls","page":"Chebyshev","text":"ChebyshevU(1) is an error: second-kind points need n ge 2.\nValues changed in 0.3.0 with the move onto 01, including the order of the nodes.\nIndexing starts at 0, as for Bernstein and Legendre, but not Lagrange.\nEvaluation outside 01 is not rejected; it extrapolates the polynomial. See Evaluation outside the domain.","title":"Pitfalls"},{"category":"section","location":"legendre/#Legendre-basis","page":"Legendre","text":"","title":"Legendre basis"},{"category":"section","location":"legendre/#Definition","page":"Legendre","text":"The Legendre polynomials P_j on -1+1 satisfy Bonnet's recurrence\n\nj  P_j(tildex) = (2j-1)  tildex  P_j-1(tildex) - (j-1)  P_j-2(tildex) \nqquad P_0 = 1  quad P_1 = tildex \n\nwhich is how they are evaluated here, iterated upwards; see Evaluation by recurrence. The basis provided is the shifted and normalised version,\n\nL_j(x) = sqrt2j+1  P_j(2x-1)  qquad j = 0 dots p ","title":"Definition"},{"category":"section","location":"legendre/#Domain-and-the-shift","page":"Legendre","text":"The argument is shifted by tildex = 2x-1 onto 01; see The reference interval.\n\njulia> l = Legendre(3);\n\njulia> axes(l, 1)\nInclusion(0 .. 1)\n\njulia> l[0.5, 1]                            # x = 0.5 is x̃ = 0, and P₁(0) = 0\n0.0\n\njulia> l[1.0, 1] ≈ sqrt(3)                  # L₁(1) = √3 P₁(1)\ntrue","title":"Domain and the shift"},{"category":"section","location":"legendre/#No-nodes","page":"Legendre","text":"Legendre is a modal basis: its coefficients are the amplitudes of an expansion, not values at points. It has no nodes, and the accessors say so rather than failing obscurely:\n\njulia> nbasis(Legendre(4)), order(Legendre(4)), degree(Legendre(4))\n(4, 4, 3)\n\njulia> nnodes(Legendre(4))\nERROR: Legendre is a modal basis and has no nodes, so nnodes is not defined for it.\n[...]\n\nSee Nodal and modal bases. To recover the coefficients of a given function, project it — which is what the orthonormality below makes cheap.","title":"No nodes"},{"category":"section","location":"legendre/#Orthonormality","page":"Legendre","text":"This is the reason to choose this basis. Since\n\nint_0^1 P_i(2x-1)  P_j(2x-1)  dx = fracdelta_ij2j+1 \n\nthe factor sqrt2j+1 in the definition makes the basis orthonormal on 01:\n\nint_0^1 L_i(x)  L_j(x)  dx = delta_ij \n\nThe mass matrix is therefore the identity, and the coefficients of an L^2 projection are simply the inner products against the basis functions — no linear system.\n\njulia> l = Legendre(5);  quad = GaussLegendreQuadrature(10);\n\njulia> M = [sum(weights(quad)[k] * l[nodes(quad)[k], i] * l[nodes(quad)[k], j]\n                for k in eachindex(nodes(quad))) for i in 0:4, j in 0:4];\n\njulia> maximum(abs, M - [i == j for i in 0:4, j in 0:4]) < 1e-14\ntrue\n\nProjecting a function is then a matter of one quadrature sum per coefficient:\n\njulia> l = Legendre(4);  quad = GaussLegendreQuadrature(10);\n\njulia> f(x) = x^3 - x;\n\njulia> c = [sum(weights(quad)[k] * f(nodes(quad)[k]) * l[nodes(quad)[k], j]\n                for k in eachindex(nodes(quad))) for j in 0:3];\n\njulia> sum(c[j+1] * l[0.3, j] for j in 0:3) ≈ f(0.3)   # exact: f has degree 3\ntrue\n\nUnlike Chebyshev, whose orthogonality needs a weight, this holds in the plain unweighted inner product.","title":"Orthonormality"},{"category":"section","location":"legendre/#Derivative","page":"Legendre","text":"Differentiating Bonnet's recurrence term by term gives\n\nj  P_j = (2j-1)  P_j-1 + (2j-1)  tildex  P_j-1 - (j-1)  P_j-2 \n\niterated upwards alongside P_j itself, which the right-hand side needs. The chain-rule factor 2 of the shift is applied at the end, and the sqrt2j+1 normalisation carries through.\n\njulia> l = Legendre(3);  d = Derivative(axes(l, 1));\n\njulia> [(d*l)[1.0, j] for j in eachindex(l)]     # 0, 2√3, 6√5\n3-element OffsetArray(::Vector{Float64}, 0:2) with eltype Float64 with indices 0:2:\n  0.0\n  3.4641016151377544\n 13.416407864998739\n\nwarning: Fixed in 0.3.0\nEarlier versions omitted the sqrt2j+1 factor here, although the basis functions carry it, so the derivative was wrong by exactly that factor for every j  0.","title":"Derivative"},{"category":"section","location":"legendre/#Pitfalls","page":"Legendre","text":"No nodes: nodes, nnodes and grid all throw. Use Lagrange or Chebyshev if a nodal basis is wanted.\nThe normalisation is part of the basis. These are not the plain P_j; every basis function is scaled by sqrt2j+1, so l[1.0, 1] is sqrt3 rather than 1.\nIndexing starts at 0, as for Bernstein and Chebyshev, but not Lagrange.\nEvaluation outside 01 extrapolates rather than erroring; see Evaluation outside the domain.","title":"Pitfalls"},{"category":"section","location":"bernstein/#Bernstein-basis","page":"Bernstein","text":"","title":"Bernstein basis"},{"category":"section","location":"bernstein/#Definition","page":"Bernstein","text":"The Bernstein basis of degree p on 01 is\n\nB_jp(x) = binompj  x^j  (1-x)^p-j  qquad j = 0 dots p \n\nFor its history and its role in computer-aided geometric design, see [7].","title":"Definition"},{"category":"section","location":"bernstein/#Domain","page":"Bernstein","text":"No shift is needed: the basis is defined on 01 to begin with, which is also this package's reference interval.\n\njulia> b = Bernstein(3);\n\njulia> axes(b, 1)\nInclusion(0 .. 1)\n\njulia> nbasis(b), order(b), degree(b)\n(3, 3, 2)","title":"Domain"},{"category":"section","location":"bernstein/#No-nodes","page":"Bernstein","text":"Bernstein is a modal basis. Its coefficients are not values of the function at points — they are the control values of a Bézier curve, which the curve approaches but in general does not pass through. So it has no nodes:\n\njulia> grid(Bernstein(4))\nERROR: Bernstein is a modal basis and has no nodes, so grid is not defined for it.\n[...]\n\nSee Nodal and modal bases.","title":"No nodes"},{"category":"section","location":"bernstein/#Partition-of-unity,-positivity,-convex-hull","page":"Bernstein","text":"On 01 the basis functions are non-negative and sum to one:\n\njulia> b = Bernstein(5);\n\njulia> all(b[x, j] ≥ 0 for x in 0:0.1:1, j in eachindex(b))\ntrue\n\njulia> sum(b[0.42, j] for j in eachindex(b)) ≈ 1\ntrue\n\nTogether these give the convex-hull property: an expansion sum_j c_j B_jp(x) is a convex combination of its coefficients, so its values lie between min_j c_j and max_j c_j for every x in the interval. No other basis here has this. It is what makes Bernstein the basis of choice when a bound on the represented function has to be guaranteed rather than merely observed, and it is the reason Bézier curves stay inside their control polygon.\n\njulia> b = Bernstein(5);  c = [0.0, 1.0, -2.0, 0.5, 3.0];\n\njulia> vals = [sum(c[j+1] * b[x, j] for j in eachindex(b)) for x in 0:0.01:1];\n\njulia> minimum(c) ≤ minimum(vals) && maximum(vals) ≤ maximum(c)\ntrue","title":"Partition of unity, positivity, convex hull"},{"category":"section","location":"bernstein/#Endpoint-interpolation","page":"Bernstein","text":"The first and last basis functions interpolate the endpoints, and all the others vanish at both:\n\njulia> b = Bernstein(4);\n\njulia> [b[0.0, j] for j in eachindex(b)]\n4-element OffsetArray(::Vector{Float64}, 0:3) with eltype Float64 with indices 0:3:\n 1.0\n 0.0\n 0.0\n 0.0\n\njulia> [b[1.0, j] for j in eachindex(b)]\n4-element OffsetArray(::Vector{Float64}, 0:3) with eltype Float64 with indices 0:3:\n 0.0\n 0.0\n 0.0\n 1.0\n\nSo an expansion begins at its first coefficient and ends at its last — convenient when boundary values have to be imposed, even though the basis is modal.","title":"Endpoint interpolation"},{"category":"section","location":"bernstein/#Derivative","page":"Bernstein","text":"The derivative of a Bernstein polynomial is a Bernstein expansion of one degree less:\n\nB_jp(x) = p  big( B_j-1p-1(x) - B_jp-1(x) big) \n\nwith the convention that B_jq vanishes outside 0 le j le q, which is what makes the j = 0 and j = p cases come out right — and what makes the degenerate Bernstein(1) case, where p-1 = -1, give zero as it should.\n\njulia> b = Bernstein(3);  d = Derivative(axes(b, 1));\n\njulia> [(d*b)[0.5, j] for j in eachindex(b)]\n3-element OffsetArray(::Vector{Float64}, 0:2) with eltype Float64 with indices 0:2:\n -1.0\n  0.0\n  1.0\n\njulia> sum((d*b)[0.42, j] for j in eachindex(b)) |> abs < 1e-14      # rows sum to zero\ntrue","title":"Derivative"},{"category":"section","location":"bernstein/#Evaluation-in-closed-form","page":"Bernstein","text":"The two-index recurrence\n\nB_jp = (1-x)  B_jp-1 + x  B_j-1p-1\n\ncannot be collapsed into a single upward sweep for one j, so this basis is evaluated from the closed form instead, with the binomial coefficient accumulated as binomp-j+kk, multiplying before dividing. The partial products are integers, so in exact arithmetic every division comes out even; in Float64 the accumulation reproduces binomial(p, j) exactly for p le 54, and from p = 55 the intermediate product outgrows the integers Float64 represents exactly and the coefficient picks up rounding.\n\nNote that / promotes, so an integer element type does not survive the accumulation: the values are floating point whenever j ge 1.\n\nwarning: Changed in 0.3.0\nEarlier versions descended the recurrence, which recomputes shared subtrees and cost O(2^p) per value — 45 ms for a single degree-24 value. Values now differ from those in the last bit for some arguments, by at most 5 times 10^-16 relative, and are on balance closer to exact.","title":"Evaluation in closed form"},{"category":"section","location":"bernstein/#Pitfalls","page":"Bernstein","text":"No nodes: nodes, nnodes and grid all throw.\nThe coefficients are not function values. An expansion generally does not pass through its coefficients — that is what makes the convex-hull property possible. If interpolation is wanted, use Lagrange.\nIndexing starts at 0, as for Chebyshev and Legendre, but not Lagrange.\nPositivity and the convex-hull property hold on 01 only. Outside it the basis extrapolates and the functions do take negative values; see Evaluation outside the domain.\nThe binomial coefficient is exact in Float64 up to p = 54, which covers any degree at which this basis is numerically useful; see Evaluation in closed form above.","title":"Pitfalls"},{"category":"section","location":"#CompactBasisFunctions","page":"Home","text":"This package provides a set of basis functions, mostly compactly supported, which are implemented as ContinuumArrays. Bases are accessed like arrays with continuous dimensions, e.g. as b[0.1, 2] to evaluate the second basis function in the point 0.1. Operations such as derivatives, inner products and mass matrices are implemented as LazyArray operations, providing a high-level linear algebra interface. Functions in a basis are represented by a lazy multiplication of the basis and a vector of coefficients, which materialises only upon evaluation of the product.","title":"CompactBasisFunctions"},{"category":"section","location":"#Installation","page":"Home","text":"CompactBasisFunctions.jl and all of its dependencies can be installed via the Julia REPL by typing\n\n]add CompactBasisFunctions","title":"Installation"},{"category":"section","location":"#The-four-bases","page":"Home","text":"All four span the polynomials of a given degree on the reference interval 01; they differ in what their coefficients mean and in what properties they guarantee.\n\nbasis coefficients are carries nodes notable for\nLagrange values at the nodes yes interpolation needs no solve\nChebyshev expansion coefficients yes node sets that defeat the Runge phenomenon\nLegendre expansion coefficients no orthonormal, so the mass matrix is I\nBernstein control values no non-negative, partition of unity, convex hull\n\nPolynomial Approximation explains the distinctions — interpolation versus projection, nodal versus modal, the reference interval, the choice of nodes — and each basis then has its own page.","title":"The four bases"},{"category":"section","location":"#Basic-usage","page":"Home","text":"Construct a basis and index it:\n\njulia> using CompactBasisFunctions\n\njulia> b = Legendre(3);\n\njulia> nbasis(b), degree(b)\n(3, 2)\n\njulia> b[0.5, 0], b[0.5, 1], b[0.5, 2]\n(1.0, 0.0, -1.118033988749895)\n\nNote that these three bases index their functions from 0; Lagrange indexes from 1. Writing eachindex(b) avoids having to remember which:\n\njulia> b[0.5, :]\n3-element OffsetArray(::Vector{Float64}, 0:2) with eltype Float64 with indices 0:2:\n  1.0\n  0.0\n -1.118033988749895\n\nDifferentiate by multiplying with a Derivative over the basis's own axis:\n\njulia> d = Derivative(axes(b, 1));\n\njulia> (d*b)[0.5, 1], (d*b)[1.0, 2]\n(3.4641016151377544, 13.416407864998739)\n\nA nodal basis makes interpolation immediate, since its coefficients are the function values:\n\njulia> b = LagrangeLobatto(3);\n\njulia> f(x) = 1 + x^2;\n\njulia> c = [f(x) for x in nodes(b)];\n\njulia> sum(c[j] * b[0.3, j] for j in eachindex(b)) ≈ f(0.3)\ntrue\n\nSee Usage for the full interface, including the array-valued indexing forms, expansions, element types and how the accessors relate to those of QuadratureRules.jl.","title":"Basic usage"},{"category":"section","location":"#References","page":"Home","text":"If you use CompactBasisFunctions.jl in your work, please consider citing it by\n\n@misc{Kraus:2020:CompactBasisFunctions,\n  title={CompactBasisFunctions.jl: Compactly supported basis functions in Julia},\n  author={Kraus, Michael},\n  year={2020},\n  howpublished={\\url{https://github.com/JuliaGNI/CompactBasisFunctions.jl}},\n  doi={10.5281/zenodo.4317806}\n}","title":"References"}]
}
